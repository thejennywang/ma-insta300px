<% if @posts.any? %>
<section class="row">
	<%= render @posts %>
</section>

<% else %>
	No posts yet
<% end %>
 
<script type="text/javascript">
  // Enable pusher logging - don't include this in production
  Pusher.log = function(message) {
    if (window.console && window.console.log) {
      window.console.log(message);
    }
  };

  var pusher = new Pusher('3522eb986cc25bcb20e2');
  var channel = pusher.subscribe('theinstagramapp_channel');

  channel.bind('new_upload', function(data) {

    $.get("/posts/" + data.id + ".json", function(post_data) {

      var newPost = Mustache.render($('#post_template').html(), post)

      $(newProfile).prependTo("main .row");
    });
  });
</script>
